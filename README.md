# ASF-CLOTHING

This repository hosts a small static site for ASF CLOTHING. The site is served from the `docs/` folder using GitHub Pages.

GitHub Pages URL: https://anmolmishra848076-sys.github.io/ASF-CLOTHING

Notes:
- Product images are placeholders located in `docs/assets/`.
- To publish actual photos, replace the placeholder files in `docs/assets/` with higher-resolution images and keep the filenames the same.
